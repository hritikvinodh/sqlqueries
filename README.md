# sqlqueries
In Task 3, I used SELECT queries to retrieve data from tables, applied WHERE conditions for filtering, used ORDER BY for sorting, and LIMIT to restrict results. This helped in efficiently extracting meaningful data from the database.
